/* Import the Poppins font from Google Fonts */
@import url("https://fonts.googleapis.com/css2?family=Poppins:wght@200;400&display=swap");

/* Reset box-sizing to border-box for all elements */
* {
  box-sizing: border-box;
}

/* Style the body element */
body {
  /* Set the background image to bg.jpg, centered and covering the entire page */
  background-image: url(bg.jpg);
  background-repeat: no-repeat;
  background-position: center center;
  background-size: cover;

  /* Set the font family to Poppins, sans-serif */
  font-family: "Poppins", sans-serif;

  /* Center the content vertically and horizontally */
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  /* Set the height to 100vh and remove margins */
  height: 100vh;
  margin: 0;
}

/* Style h1 elements */
h1 {
  /* Set the color to a purple shade */
  color: rgb(179, 131, 226);

  /* Set the font size to 10rem */
  font-size: 10rem;

  /* Center the text */
  text-align: center;

  /* Set the opacity to 0.9 */
  opacity: 0.9;
}

/* Style h2 elements inside elements with the class "thought" */
.thought h2 {
  /* Set the color to white */
  color: white;

  /* Set the opacity to 0.3 */
  opacity: 0.3;

  /* Set the font family to Segoe UI, Tahoma, Geneva, Verdana, sans-serif */
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Style form elements */
form {
  /* Add a box shadow to the form */
  box-shadow: 0 10px 10px rgba(26, 2, 2, 0.1);

  /* Set the maximum width to 100% and the width to 500px */
  max-width: 100%;
  width: 500px;
}

/* Style input elements with the class "input" */
.input {
  /* Set the color to a dark gray shade */
  color: #444;

  /* Set the font size to 2rem */
  font-size: 2rem;

  /* Add padding to the input field */
  padding: 1rem 2rem;

  /* Make the input field a block element and set its width to 100% */
  display: block;
  width: 100%;

  /* Remove the border and add a border radius of 15px */
  border: none;
  border-radius: 15px;
}

/* Style the placeholder text in input elements with the class "input" */
.input::placeholder {
  /* Set the color to a light gray shade */
  color: #d5d5d5;
}

/* Style input elements with the class "input" when they are focused */
.input:focus {
  /* Set the outline color to a purple shade */
  outline-color: rgb(179, 131, 226);
}

/* Style ul elements with the class "todos" */
.todos {
  /* Set the background color to white */
  background-color: #fff;

  /* Remove padding and margins */
  padding: 0;
  margin: 0;

  /* Remove the list style */
  list-style-type: none;
}

/* Style li elements inside ul elements with the class "todos" */
.todos li {
  /* Add a top border to each list item */
  border-top: 1px solid #e5e5e5;

  /* Make the list item a clickable element */
  cursor: pointer;

  /* Set the font size to 1.5rem */
  font-size: 1.5rem;

  /* Add padding to the list item */
  padding: 1rem 2rem;
}

/* Style li elements with the class "completed" inside ul elements with the class "todos" */
.todos li.completed {
  /* Set the color to a light gray shade */
  color: #b6b6b6;

  /* Add a strikethrough to the text */
  text-decoration: line-through;
}

/* Style small elements */
small {
  /* Set the color to a light gray shade */
  color: #b5b5b5;

  /* Add margin to the top */
  margin-top: 3rem;

  /* Center the text */
  text-align: center;

  /* Set the opacity to 0.6 */
  opacity: 0.6;
}